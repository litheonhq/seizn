{{- if .Values.seizn.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "seizn.fullname" . }}
  labels:
    {{- include "seizn.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "seizn.fullname" . }}
  minReplicas: {{ .Values.seizn.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.seizn.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.seizn.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.seizn.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.seizn.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.seizn.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}

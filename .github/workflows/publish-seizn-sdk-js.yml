name: Publish @seizn/sdk-js

on:
  push:
    tags:
      - 'sdk-js-v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g., 0.9.0-beta.1)'
        required: true
        type: string

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    name: Publish @seizn/sdk-js
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        working-directory: packages/seizn-sdk-js
        # workspace package without its own lockfile — use install
        run: npm install

      - name: Build
        working-directory: packages/seizn-sdk-js
        run: npm run build

      - name: Publish
        working-directory: packages/seizn-sdk-js
        # cwd inside the package avoids workspace-root repository auto-detect issues.
        # --provenance omitted until Trusted Publisher is configured for @seizn/sdk-js on npmjs.com.
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN_PUBLISH }}

  notify:
    needs: publish
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Notify on success
        if: needs.publish.result == 'success'
        run: echo "Successfully published @seizn/sdk-js"

      - name: Notify on failure
        if: needs.publish.result == 'failure'
        run: |
          echo "Failed to publish @seizn/sdk-js"
          exit 1
